# Run: console-v2

_Started: 2026-07-22_
_Status: shipped_

## Task

Upgrade the home console from static showcase to sleek+playful machine: fix the chrome material read, replace always-on lasers with hover-event lasers, add reflective floor + dust atmosphere, richer/fuller CRT screen content with glass streak, then a fun layer — power-on sequence, click-to-cycle screen modes (matrix rain / pong / starfield), keystroke mini-terminal with commands, idle DVD-bounce screensaver, LED power-off, late-night script, sound design (clacks/hum/zap), devtools console art. Everything from the agreed proposal EXCEPT the konami-code snake game.

## Domain

frontend

## Plan

Stage A — sleek (scene, screenshot-eyeball after each):
1. Lightformer env rework — hard bright panels + dark gaps so chrome reads as chrome (rear hump currently renders near-black)
2. Lasers → hover-only event (opacity 0 idle, sweep in with `energy`)
3. Reflective floor (drei `MeshReflectorMaterial`, low res + blur) with graceful degrade; tune ContactShadows to sit the object
4. Dust motes — one `Points` cloud drifting in the spotlight
5. Fake glass streak painted in `drawScreen` (zero GPU)
6. Extend `CRT_SCRIPT` — fill 9 rows, progress-bar/spinner/✓ beats

Stage B — screen OS (pure libs in `app/lib/` + unit tests per non-negotiable #4):
7. Screen-mode architecture: `drawScreen` becomes mode-dispatched; pure state machines for matrix-rain / pong (self-playing) / starfield / dvd-bounce screensaver
8. Power-on sequence (line-collapse expand + memory-check text) on boot dismiss
9. Click CRT (raycast on meshes) cycles modes
10. Power-LED click → CRT collapse-to-line off, click reboots
11. Mini terminal — global keystroke capture (skip when input focused), command interpreter (`help`, `whoami`, `sudo hire sean`, `clear`, …), echo on screen
12. Idle 60s → screensaver mode; any input returns
13. Late-night (00:00–05:00 local) alternate script lines

Stage C — polish:
14. Sound design — key clacks synced to typing, CRT hum, laser zap on portal hover; gated behind existing SoundToggle state (needs small shared sound state)
15. Devtools console ASCII art + `hire()` easter egg

Constraints (from brain): NO drei HDR Environment preset; DOM stage must stay complete on dead GPU; `ContextGuard` path intact; motion behind `prefers-reduced-motion`; neon green in-render only; reflector/bloom GPU risk — test, degrade gracefully.

## Baseline

```
$ ./init.sh --baseline
typecheck:     PASS
test:          PASS (30 files, 295 tests)
harness-check: PASS (11/11 invariants)
```

Tree already carries the uncommitted 2026-07-22 batch (marketplace + emoji takeover) — user-directed, commits on user word.

---

## Step 1 — pure libs + tests

9 new modules in `app/lib/`: `crt-modes`, `matrix-rain`, `pong`, `starfield`, `dvd-bounce`, `crt-terminal`, `boot-sequence`, `console-egg`, `console-sound`; `crt-script` extended (progress-bar beats, `LATE_NIGHT_SCRIPT`, `scriptForHour`). +70 unit tests. Two red-green fixes along the way: `ls` output was 36 chars vs the 34-char screen budget (test caught it); starfield clamp test updated after visibility boost (0.4→1.1 size, 0.15→0.42 bright — stars were invisible on the tube at first screenshot).

## Step 2 — scene rewrite + wiring

`hero-scene.client.tsx` rewritten around a `CrtOs` ref (mode + per-mode state + timers), mode-dispatched canvas drawing, global keydown (skips form controls/modifiers) + pointer-activity listeners, tube click cycle, LED click power ritual (invisible 0.14 hit halo added after Playwright clicks kept missing the 10px LED — real users get the same mercy), floor/fog/motes/env changes, lasers → energy-driven. `home.tsx` mounts the console egg; `sound-toggle.tsx` flips `setSoundEnabled` (same gesture unlocks the AudioContext).

Iteration findings (screenshot loop):
- Chrome env: front bezel rendered BLACK until camera-side Lightformers added — chrome mirrors the empty room. Front + top-front panels are load-bearing.
- Floor: object floated 0.36 units above the reflector → group dropped y −0.25; horizon seam killed with `<fog #141416 7→17>` + floor color #101012.
- Bloom (postprocessing) REJECTED deliberately: new dep + EffectComposer vs the HDR-context-loss history; canvas shadowBlur already glows. CRT hum also skipped — ambient.mp3 covers it.

## Step 3 — browser verification (main thread, per project memory)

Live walk on localhost:5173, 17 screenshots archived in `features/console-v2/screenshots/`. Boot BIOS ✓, attract+bars ✓, `help`/`sudo hire sean` ✓, matrix/pong/starfield ✓, click cycle ✓, LED off→dead-glass→reboot ✓, 61s saver + wake ✓, hover lasers+emoji regression ✓, egg `window.hire()` ✓, 0 console errors. Codec-less Playwright Chromium can't play mp3 → toggle correctly stays OFF (graceful path exercised); audible SFX needs one human listen in real Chrome.

## Final

_Closed: 2026-07-22_

- Shipped: uncommitted batch on `main` (user commits on their word), feat-011 → shipped
- Brain docs updated: `features/console-v2/console-v2.md` (new), `verifications/2026-07-22.md` (new), `design-language.md` (console-v2 amendment), `CHANGELOG.md`, `feature_list.json`, `progress.md`
- Left undone: none in scope. Follow-ups noted: human listen for SFX in real Chrome; konami code intentionally excluded (user)
- Surprises worth remembering: chrome-needs-front-light (black-render trap); tiny click targets need invisible hit halos; test-driven line-length budget caught real overflow
