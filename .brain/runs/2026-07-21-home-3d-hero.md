# Run: home-3d-hero

_Started: 2026-07-21_
_Status: shipped_

> Retroactive run note — feature was built in parallel with feat-009 (user-approved parallel delegation; status kept `planned` during the build to preserve the one-in-progress-at-a-time invariant, flipped straight to `shipped` at close). Logged after verify-done flagged the missing lifecycle trail.

## Task

Build feat-007: replace template landing (`app/routes/home.tsx`) with desktop.fm-style 3D hero — R3F chrome torus knot + neon lasers inside MacosFrame, monochrome UI, SF Mono corner labels, per design-language.md HOME spec.

## Domain

frontend

## Plan / Result (single delegated pass, general-purpose agent)

1. Deps: `three`, `@react-three/fiber`, `@react-three/drei`, `@types/three` (dev).
2. SSR strategy: `hero-scene.client.tsx` (`.client` module — stripped from Workers bundle by @react-router/dev, verified: 0.07 kB server stub, three.js in 928 kB lazy client chunk) wrapped by `hero-scene.tsx` (mounted flag + React.lazy + silver-mist fallback SSR-side).
3. Scene: chrome torus knot (meshPhysicalMaterial metalness 1 / roughness 0.08 / clearcoat 1, drei Environment "studio") + two #39ff14 laser cylinders (toneMapped false); idle rotation + eased mouse parallax via pure helpers `app/lib/hero-scene-utils.ts` (clamp/parallaxTarget/approach, +12 unit tests).
4. `home.tsx` rewrite: PortfolioNav (shared, from feat-009 extraction), headline 800/-0.036em, ghost "View work" → /projects + solid #111 "Marketplace" → /marketplace, corner labels, meta row, scroll cue. Dropped `redirectIfAuthenticated` (portfolio landing stays visible to signed-in users; helper still gates /login + /sign-up).
5. i18n: `home` namespace rewritten en+zh (`hero.*` keys); design-token labels untranslated.

## Baseline

Inherited session baseline (typecheck PASS, 244→256 tests during parallel feat-009 work, harness 11/11).

## Verification

- Gates: typecheck exit 0; tests 268/268; build PASS (server bundle three-free).
- feature-verifier PASS — `.brain/features/home-3d-hero/verifications/2026-07-21.md` (7/7 golden, 0 jsErrors, lazy chunk + HDR 200).
- verify-done-runner PASS functionally; brain gaps closed in this note + feature MD + i18n.md.

## Final

_Closed: 2026-07-21_

- Shipped: feat-007 (uncommitted — repo still has no commits)
- Left undone: **actual 3D pixels never machine-confirmed** — headless software-WebGL renders blank canvas despite clean mechanics (canvas attaches, assets 200, zero errors, control WebGL draw works). Needs one-time human/GPU spot check of chrome knot + lasers. Also: drei studio HDR fetched from pmndrs CDN at runtime (documented, accepted).
- Surprises worth remembering: `.client.tsx` suffix cleanly strips modules from the Workers bundle (`clientFileRE` in @react-router/dev) — canonical client-only pattern now in `rules/frontend.md`.
