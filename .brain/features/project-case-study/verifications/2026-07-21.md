# Verification: Project Case Study

- **Slug**: `project-case-study` (feature memo: `.brain/features/project-case-study/project-case-study.md`)
- **Date**: 2026-07-21
- **Verified by**: feature-verifier (Playwright CLI — headless script via `bun`)
- **Base URL**: http://localhost:5183 _(dev server started by agent on port 5183 to avoid colliding with a concurrent verifier on 5173)_
- **Role**: n/a — `/projects/:slug` is a public page, no auth required
- **Verdict**: ✅ PASS

## Golden path

`/projects/portfolio-v3` (featured=true, sortOrder=0 — full case-study fields, client set, stats present).

| # | Step | Expected | Observed | Screenshot | Result |
|---|------|----------|----------|------------|--------|
| 01 | Load `/projects/portfolio-v3` | `case-study-title` renders project title | Title text = `"portfolio-v3"` | [`01-title.png`](../screenshots/01-title.png) | ✅ |
| 02 | Meta grid | `CLIENT/ROLE/STACK/YEAR` all present (4-col grid, client is set) | Labels found: `CLIENT, ROLE, STACK, YEAR` | [`02-meta.png`](../screenshots/02-meta.png) | ✅ |
| 03 | WHY/HOW/SOLUTION sections | `case-study-why/how/solution` render with eyebrows `01 — WHY` / `02 — HOW` / `03 — SOLUTION` | All 3 sections present with matching eyebrow text | [`03-sections.png`](../screenshots/03-sections.png) | ✅ |
| 04 | TOC at 1440px | `case-study-toc` visible on `lg+` viewport | TOC visible (`isVisible() === true`) at 1440×900 | [`04-toc.png`](../screenshots/04-toc.png) | ✅ |
| 05 | Stats row | `case-study-stats` renders numeric stat values | Rendered `234+ UNIT TESTS`, `<50ms COLD START` | [`05-stats.png`](../screenshots/05-stats.png) | ✅ |
| 06 | Prev/next footer | `case-study-prev` + `case-study-next` present, hrefs point at other project slugs | `prev href=/projects/home-karaoke` (wrap-around), `next href=/projects/cf-saas-starter` | [`06-footer.png`](../screenshots/06-footer.png) | ✅ |
| 07 | Click next | Navigates to another case study | Landed on `/projects/cf-saas-starter`, title = `"CF SaaS Starter"` | [`07-next-nav.png`](../screenshots/07-next-nav.png) | ✅ |
| 08 | Mobile 390px | Golden path renders at mobile width, no crash (TOC correctly hidden per `lg:block`) | Page rendered, stacked layout, TOC not shown | [`08-mobile-390.png`](../screenshots/08-mobile-390.png) | ✅ |

## Null-field path

`/projects/day-trader` (no `client`, `why`, `how`, `solution`, `statsJson` in seed fixture).

| # | Step | Expected | Observed | Screenshot | Result |
|---|------|----------|----------|------------|--------|
| N1 | Load `/projects/day-trader` | Page renders, no crash; WHY/HOW/SOLUTION sections + stats absent; CLIENT column absent from meta grid | Title = `"Day Trader"`; `case-study-why/how/solution` count = 0; `case-study-stats` count = 0; meta labels = `ROLE, STACK, YEAR` (no `CLIENT`, grid narrowed to 3 cols); prev/next footer still present (`cf-saas-starter` / `home-karaoke`) | [`N1-null-fields.png`](../screenshots/N1-null-fields.png) | ✅ |

## Error path

`/projects/does-not-exist` — fresh unauthenticated-equivalent browser context (feature has no auth, but isolated context used per convention).

| # | Step | Expected failure surface | Observed | Screenshot | Result |
|---|------|--------------------------|----------|------------|--------|
| E1 | Load `/projects/does-not-exist` | HTTP 404, `case-study-not-found-title` + `case-study-back-to-projects` link rendered, no crash | Response status 404; title = "Project not found"; back link present | [`E1-not-found.png`](../screenshots/E1-not-found.png) | ✅ |
| E2 | Click back link | Navigates to `/projects` | Landed on `http://localhost:5183/projects` | [`E2-back-to-projects.png`](../screenshots/E2-back-to-projects.png) | ✅ |

## Console

Two full runs were made. The first run's golden-path load hit a burst of `504` responses on `/node_modules/.vite/deps/*.js` (React, tRPC, superjson, clsx, tailwind-merge) — this is the **Vite dev-server cold-start dependency pre-bundling artifact** that occurs on the very first request after `bun run dev` finishes booting, not an app defect. A second run against the now-warm server produced **zero** network errors on any golden-path or null-field step, confirming this. The verdict and linked screenshots are from the clean second run.

- **jsErrors**: none (both runs — no uncaught exceptions, no React/hydration warnings)
- **networkErrors**: none on golden path or null-field path (second/clean run). Error-path steps show the expected `404@E1` for `/projects/does-not-exist` (intentional bad slug — not a FAIL). First run's transient `504`s on golden-path step 01 were a dev-server cold-start artifact, reproduced-away on re-run; not attributable to app code.

## Findings for main thread

- None. All `data-testid`s used (`case-study-title`, `case-study-meta`, `case-study-why/how/solution`, `case-study-toc`, `case-study-stats`, `case-study-prev/next`, `case-study-not-found-title`, `case-study-back-to-projects`) existed exactly as documented in the feature memo — no missing testids.
- Prev/next wrap-around behavior confirmed live: `portfolio-v3` (first in `featured DESC, sortOrder ASC` order) shows `prev = home-karaoke` (last project, wrapped) and `next = cf-saas-starter`, matching the documented "wrap around at either end" design decision.
- CLIENT-column omission confirmed to correctly narrow the meta grid from 4 to 3 columns (visually verified in `N1-null-fields.png`) rather than leaving an empty cell.

## Verdict rationale

All 8 golden-path steps, 1 null-field step, and 2 error-path steps passed against the running dev server (port 5183, isolated from the concurrent verifier on 5173) with local D1 fixtures already seeded (`portfolio-v3`, `cf-saas-starter`, `day-trader`, `home-karaoke`). No uncaught JS errors or hydration warnings on any run. The only network errors observed were a one-time Vite cold-start artifact on the first script invocation, which vanished on a clean re-run against the warm server — not a product defect. Screenshots for every asserted state (desktop golden path steps 01–07, mobile 390px step 08, null-field day-trader, 404 + back-navigation) are saved under `features/project-case-study/screenshots/`. PASS.
