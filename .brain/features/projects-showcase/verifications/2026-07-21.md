# Verification: Projects Showcase

- **Slug**: `projects-showcase` (feature memo: `.brain/features/projects-showcase/projects-showcase.md`)
- **Date**: 2026-07-21
- **Verified by**: feature-verifier (Playwright CLI — headless script via `bun`)
- **Base URL**: http://localhost:5173 _(dev server: started by agent)_
- **Role**: public (no auth) — `/projects` is an unauthenticated public route
- **Verdict**: ✅ PASS

## Golden path

Data was already seeded in local D1 (4 fixtures present via `scripts/seed-preview.ts`'s `PROJECT_FIXTURES`: `portfolio-v3` category `saas` featured, `cf-saas-starter` category `tooling`, `day-trader` category `experiment`, `home-karaoke` category `tooling`), so `bun run db:seed` was not required to re-run.

| # | Step | Expected | Observed | Screenshot | Result |
|---|------|----------|----------|------------|--------|
| 01 | Navigate to `/projects` | `<h1>Work</h1>` renders | Title "Work" visible: true | [`01-projects-page.png`](../screenshots/01-projects-page.png) | ✅ |
| 02 | Project count label | Count label present | `data-testid="projects-count"` text: `"4 projects"` | [`01-projects-page.png`](../screenshots/01-projects-page.png) (same load) | ✅ |
| 03 | 4 seeded projects render | `portfolio-v3` full-width featured card + 3 others in 2-col grid | All 4 cards visible; `project-card-portfolio-v3` has `md:col-span-2` (full-width) | [`03-project-cards.png`](../screenshots/03-project-cards.png) | ✅ |
| 04 | Filter pills present | All + saas/tooling/experiment pills visible | `filter-all`, `filter-saas`, `filter-tooling`, `filter-experiment` all visible | [`04-filter-pills.png`](../screenshots/04-filter-pills.png) | ✅ |
| 05 | Click `saas` category pill | Grid narrows to saas-only projects | URL → `?category=saas`; grid narrowed from 4 → 1 card; `portfolio-v3` (the only `saas` fixture) visible, `cf-saas-starter` (actually category `tooling` despite its name) correctly hidden | [`05-filtered-saas.png`](../screenshots/05-filtered-saas.png) | ✅ |
| 06 | Click a project card (`day-trader`) | Navigates toward `/projects/day-trader`; detail route not built yet → 404 there is EXPECTED | `href="/projects/day-trader"` confirmed correct; click navigated to that URL; page shows app's 404 ("The requested page could not be found.") — expected, not a bug | [`06-clicked-project-card.png`](../screenshots/06-clicked-project-card.png) | ✅ |
| 07 | Mobile viewport 390px | Nav collapses gracefully, no overflow | Wordmark visible, layout stacks to single column, no horizontal scroll/clipping observed | [`07-mobile-390.png`](../screenshots/07-mobile-390.png) | ✅ |
| 08 | Hover state on a card | Border color shifts `#dddddd` → `#111` per design spec | Computed `border-color` on hover: `rgb(17, 17, 17)` = `#111111` | [`08-hover-card.png`](../screenshots/08-hover-card.png) | ✅ |

## Error path

| # | Step | Expected failure surface | Observed | Screenshot | Result |
|---|------|--------------------------|----------|------------|--------|
| E1 | Direct nav to `/projects?category=nonexistent` | Page renders without crash, empty grid state shown gracefully | `data-testid="projects-empty"` visible with copy "No projects match this filter."; 0 cards in `projects-grid`; nav/title/pills all still render normally | [`E1-empty-category.png`](../screenshots/E1-empty-category.png) | ✅ |

## Console

- **jsErrors**: none
- **networkErrors**: none

No hydration warnings observed; no `caret-color`/`caretColor` tokens found in `app/` (grep run as backstop, N/A here since no hydration warning fired anyway).

## Findings for main thread

- Minor UI nit (not a crash, not blocking): on `/projects?category=nonexistent` (E1), the `projects-count` label still reads "4 projects" (total unfiltered count) rather than reflecting the 0 visible results, and the `filter-all` pill remains visually active (black background) even though the current `category` param (`nonexistent`) doesn't match "all" or any known category. Cosmetic — worth a follow-up if the count/pill-active state should track the actual filtered result set.
- No missing `data-testid`s encountered — `project-filters`, `filter-*`, `projects-grid`, `project-card-*`, `projects-count`, `projects-empty`, `projects-wordmark`, `nav-*` all present and worked as expected.
- Confirms feature doc's self-verified claims (hover border `#dddddd → #111`, mobile nav overflow fix, filter pills) hold up under a fresh independent Playwright run.

## Verdict rationale

All 9 golden-path assertions and the 1 error-path assertion passed against the live dev server with real seeded D1 data. Zero uncaught JS errors, zero React/hydration warnings, zero network 4xx/5xx on any step (golden or error path — the intentional `?category=nonexistent` deep-link is a client-side filter with no failing request, and the intentional click-through to the not-yet-built `/projects/day-trader` detail route correctly renders the app's existing 404 page rather than crashing). Screenshots captured for every asserted state including the required mobile (390px) and hover-state checks. PASS.
