# Verification — console-v2 — 2026-07-22

**Verdict: PASS** (main-thread browser walk — design-critical work stays on the main thread per project memory; live dev server at localhost:5173, Playwright-driven Chromium 1440×900, screenshots eyeballed at every step)

## Golden path

| # | Step | Evidence | Result |
|---|------|----------|--------|
| 1 | Load `/` → CRT boot: BIOS memcheck types out (URGEL BIOS v3.0 / MEM CHECK 640K OK / …) | `screenshots/v2-t1.jpeg`, `v2-reboot.jpeg` | PASS |
| 2 | Attract terminal types agentic script with ▓ progress bars; motes drift; floor reflects; lasers dark at idle | `v2-boot.jpeg`, `v2-early.jpeg` | PASS |
| 3 | Type `help` ⏎ from bare page → interactive terminal opens, command list renders inside 34-char rows | `v2-term-help.jpeg` | PASS |
| 4 | `sudo hire sean` ⏎ → PERMISSION GRANTED ✓ + email | `v2-term-sudo.jpeg` | PASS |
| 5 | `matrix` ⏎ → rain; click tube → pong (paddles, net, score); click → starfield | `v2-matrix.jpeg`, `v2-pong.jpeg`, `v2-starfield2.jpeg` | PASS |
| 6 | Click power LED → collapse animation → dead glass tube; click again → BIOS reboot | `v2-off.jpeg`, `v2-reboot.jpeg` | PASS |
| 7 | 61s hands-off → DVD screensaver (SEAN box + corners counter); mouse move wakes to attract | `v2-saver.jpeg`, `v2-wake.jpeg` | PASS |
| 8 | Hover Work portal → lasers sweep in + 🛠️ phosphor takeover + portal inversion (regression) | `v2-hover.jpeg` | PASS |
| 9 | Devtools egg: `window.__crtEggPrinted === true`, `window.hire()` returns `sean@casperstudios.xyz` | evaluate result in run log | PASS |

## Error paths
- Typing with a focused form control / modifier keys → terminal does not capture (code path; no other inputs exist on `/` to conflict).
- Sound toggle in codec-less Chromium: `audio.play()` rejects → toggle stays OFF, **zero console errors** (graceful-degrade path exercised for real; mp3 plays in real Chrome — pre-existing shipped behavior).
- Dead-GPU path unchanged: DOM stage + portals complete without WebGL; ContextGuard + 5s boot timeout intact.

## Caveats
- Audible SFX (clack/zap) not machine-verifiable headless — code no-ops cleanly; needs a one-time human listen in real Chrome.
- Late-night script covered by unit tests (`scriptForHour`), not by browser time-travel.
- Power-off mid-collapse frame not captured (1s animation vs screenshot latency); collapse math unit-tested, end state verified.

Console: 0 errors across the whole walk.
