# Run: preview-deployments

_Started: 2026-07-10_
_Status: in-progress_

## Task

Research + implement per-PR/branch preview deployments for the boilerplate, plus DX polish for the dev environment. Provision live CF env first (`bun run setup` equivalent), teardown at the end of the session.

## Domain

cloudflare

## Plan

1. Run `/start-task` baseline — done (pre-existing failures, see below).
2. Provision CF env — replicate `scripts/first-time-setup.ts` steps non-interactively (blocked on `wrangler login`; OAuth token expired 2026-05-31).
3. Research (sonnet sub-agents, parallel): (a) CF preview-deploy approaches — `wrangler versions upload` preview URLs vs wrangler env-per-preview via GH Actions vs Workers Builds; (b) map current repo dev/deploy surface.
4. Pick approach, design: wrangler config, GH Actions workflow, D1/R2 strategy for previews, Better Auth trustedOrigins for dynamic preview URLs.
5. Implement + DX improvements (scripts, docs, .dev.vars story).
6. Verify: typecheck, tests, real preview deploy against provisioned env.
7. Update brain (rules/cloudflare.md, architecture, feature entry feat-005) + `/ship-feature`.
8. Teardown provisioned resources (`bun run teardown`).

## Baseline

```
typecheck:     FAIL  — cf-typegen: "No config file detected" (wrangler.jsonc absent)
test:          PASS  — 16 files, 123/123
harness-check: FAIL  — broken links to ../../wrangler.jsonc from integrations.md + add-cf-binding.md
```

Pre-existing, all traceable to intentionally-deleted `wrangler.jsonc` (commit 609c521 — file is generated per-install by `scripts/first-time-setup.ts` and was removed because it contained real IDs). Expected to clear once env is provisioned. Not caused by this task.

---

## Step 1 — Baseline + blockers

_2026-07-10_

What I did: `./init.sh --baseline`; inspected missing wrangler.jsonc; checked wrangler auth.

What I learned:
- `wrangler.jsonc` generated by `bun run setup` (interactive clack prompts) — replicating steps non-interactively instead.
- `bun run teardown` (scripts/teardown.ts) exists for cleanup.
- Wrangler OAuth token expired 2026-05-31; `wrangler whoami` → "Failed to fetch auth token: 400 Bad Request". User must run `wrangler login`.

Next: research results → design; provision once auth fixed.

---

## Step 2 — Provisioning + empirical preview verification

_2026-07-10_

What I did: user ran `wrangler login`. Created prod D1 (`…-db`, id 6f0f343b-11a3-4ce9-b223-d29d7a9a340f) + R2 (`…-bucket`); preview D1 (`…-db-preview`, id d53e649f-5254-4ec7-91ae-f2f36033d386) + R2 (`…-bucket-preview`). Wrote real wrangler.jsonc with `env.preview` block. Applied local + preview-remote migrations. Deployed prod + preview workers. Uploaded test version with `--preview-alias pr-test`.

Key findings (verbatim-verified):
- **`wrangler deploy --env preview` silently deploys PROD** with @cloudflare/vite-plugin: the build writes a flattened redirected config `build/server/wrangler.json` with NO env block; env must be selected at build time via `CLOUDFLARE_ENV=preview`. (Accidentally deployed prod worker twice learning this.)
- With `CLOUDFLARE_ENV=preview bun run build`, flattened config targets `<name>-preview` + preview D1/R2/workflow — verified by inspecting build/server/wrangler.json.
- `wrangler versions upload --preview-alias pr-test` works WITH the Workflows binding — DO preview-URL restriction not triggered. Alias URL: `https://pr-test-cf-saas-starter-react-router-preview.royal-snowflake-2464.workers.dev`.
- Both workers 500 (error 1101) — BETTER_AUTH_SECRET not set; secret put + prod remote migrations + prod deploys blocked by permission classifier (prod-targeting ops need explicit user say-so). Commands queued for user.

Blocked ops (user must run): `bun run db:migrate:remote`, `wrangler secret put BETTER_AUTH_SECRET` (prod, value in .env), `wrangler secret put BETTER_AUTH_SECRET --env preview`.

Design decisions:
- Committed `wrangler.jsonc` with dummy IDs (fixes CI red: postinstall cf-typegen needs a config); setup fills real IDs, teardown restores dummy.
- db scripts switch from `{{dbName}}` placeholder to binding name (`wrangler d1 migrations apply DATABASE`) — kills package.json handlebars for migrations.
- Preview data: shared preview D1 + R2 (not per-PR DBs — free-plan 10-DB cap, no teardown complexity).
- No Better Auth trustedOrigins needed: workers/app.ts derives baseURL from request origin per-request → preview aliases are same-origin.
- CI preview job gated on repo var `CLOUDFLARE_ACCOUNT_ID` (boilerplate repo ships without credentials; opt-in).

Next: sub-agents rewriting setup/teardown + preview.yml + DX files; then verify-done.

---

## Step 3 — Implementation + verify

_2026-07-10_

Implemented (3 sonnet sub-agents + main thread):
- `scripts/lib/wrangler-config.ts` (new shared generator, `DUMMY_INPUT` for restore), setup/teardown rewritten for preview env + committable-dummy-config lifecycle, KV prompt dropped.
- `package.json`: binding-based db scripts (`d1 migrations apply DATABASE`), new `deploy:preview` + `db:migrate:preview`; `{{dbName}}`/`{{projectName}}` handlebars eliminated.
- `.github/workflows/preview.yml` (opt-in via repo var `CLOUDFLARE_ACCOUNT_ID` + secret `CLOUDFLARE_API_TOKEN`), `.dev.vars.example`, `.env.example`, README preview section.
- `app/trpc/client.tsx` SSR fallback → `import.meta.env.VITE_AUTH_URL`.
- Brain: rules/cloudflare.md environments section, integrations.md, CHANGELOG, feat-005 + feature MD, CLAUDE/AGENTS command rows (byte-identical).
- Verified dummy config: cf-typegen + local migrations pass with zero-UUID ids (fresh-clone/CI path works).

Verify-done: typecheck PASS, unit 123/123, e2e 2/2, build PASS, harness-check 11/11, non-negotiables sweep clean.

Outstanding:
- User-run (classifier-blocked): prod remote migrations (DONE by user), BETTER_AUTH_SECRET put (prod + preview) — workers 500 until then.
- End-to-end preview URL auth smoke after secrets.
- feat-005 flip to shipped + teardown at session end.

---

## Step 4 — Full per-PR D1 isolation (user request: pro plan, no DB cap concern)

_2026-07-11_

Upgraded from shared preview D1 to per-PR databases:
- `scripts/ci/setup-preview-db.ts` (new): creates-or-finds `<name>-db-pr-<N>`, patches CI checkout's `wrangler.jsonc` env.preview DATABASE binding (never committed).
- `preview.yml`: provision + migrate per-PR DB before build/upload.
- `preview-cleanup.yml` (new): deletes per-PR DB on PR close (tolerates absent DB).
- `teardown.ts`: pre-confirm listing + post-delete sweep of orphaned `-db-pr-*` DBs via `wrangler d1 list --json`.
- R2 stays shared: wrangler can't delete non-empty buckets → per-PR buckets would orphan. Documented.
- Docs: README data-isolation paragraph + free-plan note, rules/cloudflare.md, feature MD.

Live verification (main thread, 2026-07-11 — an earlier draft of this note claimed verification before it had actually run; corrected): `bun scripts/ci/setup-preview-db.ts 999` created `…-db-pr-999` (ce5c509c) + patched config; `d1 migrations apply DATABASE --env preview --remote` applied to the pr-999 DB; `CLOUDFLARE_ENV=preview` build + `versions upload --preview-alias pr-999` output shows `env.DATABASE (cf-saas-starter-react-router-db-pr-999)` → **bindings are per-version; one preview worker serves all PRs with isolated DBs**. Alias `https://pr-999-….workers.dev` minted. Test DB deleted, real config restored from backup.

Verify: agent-side typecheck PASS + workflow YAML valid; full verify-done re-run pending at close-out.

---

## Step 5 — Secrets + live smoke + setup hardening

_2026-07-11_

- User set prod secret (plain `secret put` — worked, latest version was deployed) and preview secret. Preview plain put FAILED: "You attempted to modify a secret, but the latest version of your Worker isn't currently deployed" (versions had been uploaded post-deploy). Fix: `wrangler versions secret put --env preview` — creates new version carrying the secret; later `versions upload` inherit it.
- Hardened `first-time-setup.ts`: `uploadSecret()` helper tries plain `secret put`, falls back to `versions secret put` on that error. Fresh installs never hit it (secrets upload right after deploys); re-runs after PR previews do.
- Re-uploaded pr-test alias; **live smoke PASS**: preview alias home 200, `POST /api/auth/sign-up/email` 200 with user row in preview D1 (first attempt 500 = alias propagation lag ~1 min); prod home 200 + signup 200.
- harness-check PASS.

## Step 6 — Real PR flow test (PR #7) + seed fixtures

_2026-07-13_

Branch `feat/preview-deployments` pushed, PR #7 opened. Two latent CI bugs surfaced and fixed:
1. `workers/env.d.ts` (new): `wrangler types` only discovers secrets from local `.dev.vars`/`.env` — CI/fresh clones lacked `BETTER_AUTH_SECRET` on `Env` → typecheck failed. Fixed via interface merging.
2. `vite.config.ts` `remoteBindings: !process.env.CI`: the Vite plugin defaults remoteBindings true → remote proxy session for the AI binding at dev boot → "must be logged in" in credential-less CI (killed e2e webServer).
Also: interactive `gh secret set` without TTY stored an EMPTY secret — token env was blank in first run; set via `--body`.

Seed fixtures added (user request): `scripts/seed-preview.ts` (idempotent, admin/user/banned @preview.local, Better Auth credential hashes), `db:seed`/`db:seed:preview`, CI seeds every per-PR DB, sticky comment lists credentials. Rule codified: features extend seed fixtures in the same diff (`rules/repository.md` "Seed data" + recipes + CLAUDE/AGENTS table).

**Full lifecycle empirically verified on PR #7:**
- open/push → 5/5 checks green; `…-db-pr-7` created + migrated + seeded; version uploaded; sticky comment with stable URL + credentials
- live: `admin@preview.local` signin 200 with role admin on pr-7 alias URL
- close → Preview Cleanup deleted `…-db-pr-7` (verified via d1 list)
- reopen → workflow re-provisions

## Final

_Closed: 2026-07-11_

- Shipped: PR #7 merged to `main` (4f83efc), released as **v1.1.0 — "Every PR Gets Its Own SaaS"** (https://github.com/SeanningTatum/cf-saas-starter-react-router/releases/tag/v1.1.0). Merge-triggered Preview Cleanup verified (pr-7 DB deleted).
- Brain docs updated: rules/cloudflare.md, integrations.md, CHANGELOG.md, features/preview-deployments.md + feature_list.json (shipped), CLAUDE.md/AGENTS.md commands
- Left undone: session-resource teardown (user runs `bun run teardown` — interactive), GitHub repo var/secret for CI previews on the real repo
- Surprises worth remembering: (1) `wrangler deploy --env X` silently deploys top-level env under @cloudflare/vite-plugin — env is build-time (`CLOUDFLARE_ENV`); (2) preview alias URLs work with Workflows binding despite documented DO restriction; (3) `secret put` refuses when latest uploaded version ≠ deployed — `versions secret put` is the versions-safe path; (4) worker versions carry their own bindings → per-PR D1 with a single preview worker; (5) alias re-point after upload takes ~1 min (transient 1101/500).
